# python-roadmap

## Versiones
- Python: 3.12.x (asdf)
- VS Code: extensiones Python, Pylance, GitLens

## Crear y activar entorno
```bash
python -m venv .venv
source .venv/bin/activate
python -V
pip list
```

## Calidad de código
```bash
pre-commit run -a
```
